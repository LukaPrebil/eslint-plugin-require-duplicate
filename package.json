{
  "name": "eslint-plugin-require-duplicate",
  "version": "1.0.0",
  "description": "ESLint plugin that spots duplicate imports using the require(\"...\") syntax.",
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "repository": "https://github.com/LukaPrebil/eslint-plugin-require-duplicate",
  "author": "Luka Prebil Grintal",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha tests --recursive",
    "lint": "eslint --ext .js lib/ tests/"
  },
  "dependencies": {
    "requireindex": "~1.1.0"
  },
  "devDependencies": {
    "eslint": "^7.18.0",
    "eslint-config-google": "^0.14.0",
    "mocha": "^7.2.0"
  },
  "peerDependencies": {
    "eslint": ">=0.8.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "MIT"
}
